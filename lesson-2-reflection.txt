What happens when you initialize a repository? Why do you need to do it?

    Creates a .git folder, to store the changes

How is the staging area different from the working directory and the repository?
What value do you think it offers?

    This is to be able to check for only one file, it offers the hability to save
    the changes only when you want to place all on one commit and select which files
    will have a specific comment.

How can you use the staging area to make sure you have one commit per logical
change?

    Only adding one file if it's involved

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?

    When you want to make some experimetal features and if you want to make other versions.
    It will be helpful to avoid loose the logical changes between several versions.

How do the diagrams help you visualize the branch structure?

    To make readable, understanding how the branches depended on the other commits

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?

    The result is the changes (adds and remove the lines depending on the parent reference).
    because it is easier to understand.

What are the pros and cons of Git's automatic merging vs. always doing merges
manually?

    The pro is to get it faster, the cons is that you can crash the code if you do not 
    have enough care.
